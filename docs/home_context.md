
**Figma: SeniorCare — node 535:1851 (Homepage - Elderly)**

![Homepage screenshot](https://www.figma.com/api/mcp/asset/7ce6f019-14aa-46cd-a4ec-236a4aff2809)

## 1) High-level summary
- Frame: `Homepage - Elderly` (id `535:1851`). Mobile artboard sized 360 x 1316.
- Purpose: senior-focused home dashboard with greeting, SOS CTA, upcoming medication card, quick-action tiles, and health summary + bottom navigation.

## 2) Components hierarchy (top → nested)
- `535:1851` Frame `Homepage - Elderly`
	- `535:1860` Instance `Mode=Light, Type=Default` (status bar / notch)
		- groups: `Right Side` (battery/wifi/signal), `Left Side` (time)
	- `535:1861` Group: top icons (back arrow, notification)
	- `536:4227` Group: 4 quick-action cards container (grid of two columns)
		- each card: rounded rect, icon (Ellipse 249), title text, calendar instance
		- icons are component instances from `Medical Icons` and `calendar` component set
	- `536:2197` Frame: floating health summary (image background + vitals text)
	- `536:4163` Navbar Frame: bottom capsule nav with 5 nav items (each a frame with icon container + label)
	- `536:4268` Group: greeting hero + SOS CTA + avatar illustration
	- `538:4269` Group: main content panel (white rounded card) with upcoming medication and detail card

Notes: Many iconography elements are component instances (component IDs shown in metadata). Several groups use image assets (imageRef values in metadata).

## 3) Design tokens & variables (extracted from Figma globalVars)
- Colors / fills:
	- `#F1F8E9` (background) — fill_P6YE1J
	- `#FFFFFF` (surface / cards) — fill_2M0W2K
	- `#000000` (primary black / typography) — fill_U7LZR6
	- `Color/Primary: #5F33E1` (accent)
	- `#4DB6AC` (green-ish) — fill_4PGCDT
	- `#A0FFD5` (Green/200)
	- Several image fills (imageRef values) used for artwork and photos
- Shadows / effects:
	- `effect_ZS3VI5`: box-shadow 0px 4px 10px rgba(0,0,0,0.25) — used on cards
	- Complex 3D inner/dropshadow tokens (named `Second 3d`, `Green 3D`) in metadata — used for stylized elements
- Typography (named styles):
	- `style_MAQJSS`: Lexend, 18pt, weight 700, center — used for card titles
	- `style_ZVHPF7`: Lexend, 20pt, weight 700 — section heading
	- `style_LVCGSZ`: Lexend, 17pt, weight 700 — numeric stat
	- `style_2PE3HI`: Lexend, 24pt, weight 700 — large stat
	- `style_A0AN4H`: Lexend, 12pt, weight 300 — body/description

## 4) Layout sections & measurements
- Artboard: width 360, height 1316.
- Status bar: instance `535:1860` width 364 height 41; left/right groups positioned absolutely.
- Hero / greeting block: centered group near top with avatar image and greeting text (assets: `imgImage1`, `imgImage2`).
- SOS CTA: large rounded rect with background `#d77658` (example); sits overlapping hero and main card area.
- Main card (white rounded card): located at ~y=406, size ~331 x 234 (layout_N3DF6T / layout_IYKM5K), contains medication card with image at right and CTA button.
- Quick-action cards: four 162x171 cards arranged in two columns; card corner radius 27, inner icon ellipse 15x15 located top-right; spacing as in layout positioning (x/y values in metadata)
- Bottom navbar: capsule shape width 379 x 86 (layout_87ZJQN) with 5 nav items spaced by gap 24px; each nav item frame width 38 x 50 and icon container 32x32 (rounded 100px)

## 5) Auto-layout properties we can rely on
From metadata `layout_*` entries indicating auto-layout where used:
- `layout_87ZJQN` (Navbar): mode: row; justifyContent: stretch; alignItems: stretch; gap: 24px; sizing: horizontal/vertical fixed. This maps to a horizontal container with evenly spaced items and explicit gap.
- Nav item frames (e.g., `layout_8CRA4W`, `layout_8119KM` etc): column mode with justifyContent center / flex-end and alignItems center — typical icon above label layout.
- Card grid items are positioned absolutely in the frame (no explicit auto-layout rows), but cards themselves are fixed-size rounded rectangles with inner elements absolutely positioned.

## 6) Assets / images (downloadable URLs)
The MCP design context provided direct asset URLs. Key ones:
- Main screenshot / frame image: https://www.figma.com/api/mcp/asset/7ce6f019-14aa-46cd-a4ec-236a4aff2809
- Avatar / illustrations: see `imgImage1`, `imgImage2`, `imgHp1RemovebgPreview1` (URLs embedded in design context)
- Icons: `imgVuesaxBulkCalendar`, `imgVuesaxBulkProfile2User`, `imgVuesaxBulkDocumentText`, `imgVuesaxBoldNotification`

Saved screenshot reference (local pointer): `docs/figma_home_url.txt` contains the primary asset URL.

## 7) Code snippets (auto-generated by Figma MCP)
Figma returned a React + Tailwind JSX export for the node. Key excerpts (trimmed):

```jsx
const img = "https://www.figma.com/api/mcp/asset/7ce6f019-14aa-46cd-a4ec-236a4aff2809";
const imgEllipse249 = "https://www.figma.com/api/mcp/asset/f68d8300-be57-4c66-8e5d-9103852af915";
// ... other assets

export default function HomepageElderly() {
	return (
		<div className="bg-[var(--light-green,#f1f8e9)] relative size-full" data-name="Homepage - Elderly">
			{/* Status bar instance */}
			<div className="absolute h-[41px] left-1/2 top-[-3px] w-[364px]">...</div>
			{/* Hero + SOS + Cards + Navbar */}
		</div>
	);
}
```

IMPORTANT: the generated code is React+Tailwind and includes `data-node-id` attributes. The design context includes many image constants that reference Figma asset URLs. If you want this converted to Flutter widgets, I can translate the structure, map design tokens to `lib/core/theme/` tokens, and create reusable Flutter components (e.g., `StatusBar`, `HeroCard`, `ActionTile`, `BottomNav`).

## 8) Implementation notes / mapping suggestions for this codebase
- Theme mapping: add tokens in `lib/core/theme/colors.dart` for `background`, `primary`, `cardBg`, `mutedText`, and shadows (cardShadow).
- Typography: map Figma style names to `TextStyle` constants in `lib/core/theme/text_styles.dart` (Lexend font family, sizes listed above).
- Components to implement in Flutter:
	- `StatusBarWidget` — absolute top,
	- `GreetingHero` — avatar + greeting + SOS button (overlapping layout using Stack),
	- `ActionCard` — rounded card with image + title + small calendar/icon badge,
	- `UpcomingCard` — the medication card with pill image and `PrimaryButton` for action,
	- `BottomCapsuleNav` — custom bottom nav with 5 items and selected state.
- Layout approach: use `Stack` for overlapping hero/SOS/main card; use `GridView` or `Wrap` for two-column quick-action tiles; use `Container` with `BoxShadow` for card shadows.

## 9) Next steps I can take (pick one)
- Convert the React+Tailwind export into a Flutter widget tree scaffold (Dart files) and theme tokens.
- Generate `lib/docs/figma_assets.md` with direct asset URLs and suggested local filenames.
- Download the highest-priority asset and add it to `assets/images/figma_home.png` and update `pubspec.yaml`.

---

Generated from Figma MCP data for node `535:1851`. Ask me which next step you want me to do and I will proceed (I can convert to Flutter widgets automatically if you want).
